{
  "name": "@flogo-agent/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsx watch src/dev-server.ts",
    "start": "node dist/dev-server.js",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@azure/functions": "^4.11.2",
    "@flogo-agent/contracts": "workspace:*",
    "durable-functions": "^3.3.0",
    "fastify": "^5.2.1"
  }
}
