## Highlights

- expands the canonical first-party Flogo reference corpus used by the agent and aligns both repo and embedded manifests to that source-of-truth set
- removes the local research report from the default indexed corpus so retrieval stays grounded in official docs, first-party repos, and official labs/examples
- improves repair-review-apply conversation fidelity by making repair, diff, and approval summaries deterministic and evidence-driven

## Validation

- hermetic e2e CLI and daemon tests passed before release
- full `go test ./...` suite passed before release
- release assets were built locally with `scripts/build-release.sh`
