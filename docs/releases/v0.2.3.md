## Highlights

- Reworked the terminal UI around a shared renderer so the live app and the screenshot review harness use the same layout.
- Improved the chat-first hierarchy with a compact status header, clearer review banners, a denser context panel, and a table-based session picker.
- Upgraded the patch review pane to emphasize semantic Flogo changes and location-based review context, then refreshed the screenshot harness assertions to match the new UI.

## Verification

- `go test ./...`
- `go run ./cmd/flogo-agent ui-review --repo . --out-dir /tmp/flogo-agent-ui-review-iter11`
