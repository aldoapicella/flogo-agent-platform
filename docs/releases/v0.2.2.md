## Highlights

- fixes the PTY bootstrap e2e harness so the root terminal UI prompt works when `TERM` is unset, matching the GitHub Actions environment that was failing CI
- records explicit user approval and rejection turns in the session transcript so live conversation evaluation can trace approval-to-apply behavior cleanly
- improves diff review replies by focusing on semantic Flogo changes instead of noisy JSON reordering

## Validation

- `TERM= go test ./e2e -run TestRootCommandShowsModelAPIKeyPromptOnTTYE2E -v`
- `OPENAI_E2E=1 go test ./e2e -run 'TestLiveOpenAI.*' -v`
- `go test ./...`
- `VERSION=v0.2.2 ./scripts/build-release.sh`
