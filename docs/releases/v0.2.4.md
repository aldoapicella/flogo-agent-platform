## Highlights

- Added an observation-driven conversational path for operational questions like local run and test guidance, including descriptor inspection, runtime trigger inspection, build-artifact inspection, and local test plan generation.
- The agent now answers local-testing questions from explicit facts such as REST port, method, route, executable path, and `-test` flag support instead of rephrasing the last generic verification summary.
- Updated the TUI context/banner and the project docs to reflect the new inspection flow, then added planner, responder, runtime, and Flogo inspection coverage for that behavior.

## Verification

- `go test ./...`
- `export PATH="$PWD/.tools/bin:$PATH"; FLOGO_INTEGRATION=1 go test -tags=integration ./internal/tools -v`
