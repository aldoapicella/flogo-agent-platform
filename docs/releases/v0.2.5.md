## Highlights

- Fixed the live OpenAI repair conversation flow so mixed inspect+repair turns are classified as `repair`, not `inspection`.
- This restores the intended repair-turn response path: the agent now explains the detected validation issues, proposed patch, and approval requirement before the diff/approval turn instead of only restating descriptor facts.
- Added a regression test for the mixed-step turn classification that caused the release-gate failure.

## Verification

- `go test ./...`
- `set -a; source .env; set +a; export PATH="$PWD/.tools/bin:$PATH"; OPENAI_E2E=1 go test ./e2e -run TestLiveOpenAIRepairConversationE2E -v`
- `set -a; source .env; set +a; export PATH="$PWD/.tools/bin:$PATH"; OPENAI_E2E=1 go test ./e2e -run 'TestLiveOpenAI.*' -v`
